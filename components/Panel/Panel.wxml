<view class="panel" wx:if="{{status}}">
    <view class='panel-content ^panel-content ~panel-content'>
        <slot />
    </view>
    <view class='icon-clear' wx:if="{{showClose}}" bindtap='close'></view>
    <view class='panel-mask' bindtap='close'></view>
</view>